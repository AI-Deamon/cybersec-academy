# Day 8 — Version History

- **v1.0** — Initial lesson built from Course Design Document (Phase 2). The **Windows lens** of the Week 2 OS signature diagram (User→Shell→OS→CPU/RAM/Disk): "same OS, different dialect" framing (drives/ACLs/PowerShell vs `/`/rwx/Bash), PowerShell read-only cmdlets (`whoami`/`Get-LocalUser`/`Get-Acl`/`Get-ChildItem`), ACLs as the Windows label of the key cabinet (parallel to Linux `rwx`), users/groups + Active Directory (local vs domain roster), UAC & least privilege (Windows `sudo`; Admin = master key), and the security bridge (AD = crown jewel; "Everyone: Full Control" = Windows `777`; forward to Day 16/18). Uses the ONE unified Restaurant→Computer metaphor (ACL = key cabinet, PowerShell = walkie-talkie). Includes safe read-only PowerShell lab, quiz, references, rubric. Follows Day-1 7-file template + Week-1 patterns. Third day of Week 2.

## How to update
Append a new line after each review cycle or cohort:
- **v1.x** – <what changed and why>
- **v2.0** – Major redesign after N student batches
