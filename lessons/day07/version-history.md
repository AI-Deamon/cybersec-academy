# Day 7 — Version History

- **v1.0** — Initial lesson built from Course Design Document (Phase 2). The **Linux lens** of the Week 2 OS signature diagram (User→Shell→OS→CPU/RAM/Disk): single `/` filesystem tree, Bash refresher, permission bits (u/g/o × r/w/x, worked `rwxr-xr--` example), users/groups (`whoami`, `root`, `sudo`, `/etc/passwd`), `chmod` on own files (vs `chown`), and the security bridge (least privilege; `777` = key in the door; forward to Day 16). Uses the ONE unified Restaurant→Computer metaphor (key cabinet = `rwx` bits). Includes safe lab (`ls -l` + `chmod +x` on own file), quiz, references, rubric. Follows Day-1 7-file template + Week-1 patterns. Second day of Week 2.

## How to update
Append a new line after each review cycle or cohort:
- **v1.x** – <what changed and why>
- **v2.0** – Major redesign after N student batches
