# Day 2 — Version History

- **v1.0** — Initial lesson built from Course Design Document (Phase 2). Covers how programs run: CPU/registers/RAM, processes & isolation, kernel vs user privilege, and the security bridge (buffer overflow → RCE). Includes lab (process view + safe end), quiz, references, rubric. Follows the Day-1 7-file template (v1.3) with the knowledge/teaching/presentation architecture.
- **v1.1** — Owner-approved refinements (rated 9.5/10): (1) expanded the chef analogy into one end-to-end **Restaurant analogy** (source→compiler→binary→CPU/RAM/disk/OS) reused across the course; (2) added the **"How a Program Starts"** 6-step pipeline slide before the buffer-overflow discussion; (3) closed with a **curiosity question** (how malware steals passwords across process boundaries) that defers to a later lesson. Kept buffer-overflow conceptual (no stack/assembly). Lab kept on student's own machine per owner direction.
- **v1.3** — Consistency update for the official 5-session week cadence (ADD §12): Day 5 is the reinforcement session (review + assignment briefing + guided planning); assignment completed over the weekend, submitted before next Monday. **No lesson content changed** — cross-reference only.
- **🧊 v1.3 = FROZEN — Week 1 Production Ready v1.0 (ADD §16 Freeze Rule):** Week 1 is locked. Do not refine from new ideas; improve only after post-teaching student evidence.

## How to update
Append a new line after each review cycle or cohort:
- **v1.x** – <what changed and why>
- **v2.0** – Major redesign after N student batches
